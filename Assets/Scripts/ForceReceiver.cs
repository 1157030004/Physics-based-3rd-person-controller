using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ForceReceiver : MonoBehaviour
{

    // Rigidbody rb;
    // Animator anim;
    // public Transform MainCameraTransform;

    // [Header("States")]
    // [SerializeField]  bool isSprinting;
    // [SerializeField]  bool isInAir ;
    // [SerializeField]  bool isGrounded ;
    // [SerializeField]  bool isInteracting ;

    // [SerializeField] LayerMask IgnoreForGroundCheck;



    // public float groundSlopeAngle = 0f;            // Angle of the slope in degrees
    // public Vector3 groundSlopeDir = Vector3.zero;  // The calculated slope as a vector

    // [Header("Settings")]
    // [SerializeField] bool showDebug = true;                  // Show debug gizmos and lines
    // [SerializeField] LayerMask castingMask;                  // Layer mask for casts. You'll want to ignore the player.
    // [SerializeField] float distanceFromBottom = -0.15f;   // Should probably be higher than skin width
    // [SerializeField] float distanceBeforeGrounded = .2f;   // Should probably be higher than skin width
    // [SerializeField] float sphereCastRadius = 0.25f;
    // [SerializeField] float sphereCastDistance = 0.75f;       // How far spherecast moves down from origin point

    // [SerializeField] float raycastLength = 0.75f;
    // [SerializeField] Vector3 rayOriginOffset1 = new Vector3(-0.2f, 0f, 0.16f);
    // [SerializeField] Vector3 rayOriginOffset2 = new Vector3(0.2f, 0f, -0.16f);
    // [SerializeField] float gravityScale;

    // [SerializeField] float originOffset;
    // [SerializeField] float rayLength;
    // [SerializeField] float rideHeight;
    // [SerializeField] float rideSpringStrength;
    // [SerializeField] float rideSpringDamper;
    // [SerializeField] float uprightJointSpringStrength;
    // [SerializeField] float  uprightJointSpringDamper;



    // Vector3 normVector;
    // Vector3 targetPos;
    // float inAirTime;
    // float moveAmount;

    // float verticalVelocity;
    // float distance;

    // bool beforeGrounded;
    // bool rayDidHit;
    

    // public Vector3 Movement => Vector3.up * verticalVelocity;

    // void Start()
    // {
    //     rb = GetComponent<Rigidbody>();
    //     anim = GetComponent<Animator>();
    //     MainCameraTransform = Camera.main.transform;

    // }



    // void AnimationHandler(string targetAnimation, float duration)
    // {
    //     anim.CrossFade(targetAnimation, 0.2f);
    //     anim.PlayInFixedTime(targetAnimation, 0, .25f);
    // }

}

